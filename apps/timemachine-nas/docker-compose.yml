services:
  samba:
    image: crazymax/samba:latest
    network_mode: host
    environment:
      - TZ=Etc/UTC
      - SAMBA_LOG_LEVEL=1
    volumes:
      - ${APP_DATA_DIR}/data:/data
    x-runtipi:
      is_main: true
      internal_port: 445

x-runtipi:
  schema_version: 2
