{
  "schemaVersion": 2,
  "services": [
    {
      "name": "lm-studio",
      "image": "lms-headless:latest",
      "isMain": true,
      "internalPort": 1234,
      "volumes": [
        {
          "hostPath": "${APP_DATA_DIR}/models",
          "containerPath": "/root/.cache/lm-studio"
        }
      ],
      "environment": [
        { "key": "MODEL_ID", "value": "${MODEL_ID}" },
        { "key": "PORT", "value": "${PORT}" },
        { "key": "CUDA_VISIBLE_DEVICES", "value": "${CUDA_VISIBLE_DEVICES}" }
      ]
    }
  ]
}
